#include <stdio.h>
/**
 * main: entry point
 * description: print base 10 numbers
 * Return: 0 if successful
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf (x'\n');
	}
	return (0);
