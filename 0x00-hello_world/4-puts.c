#include <stdio>

/**
 * main : this is the entry point for the program
 *
 * return: this is the exit point. It returns zero if positive and non-zero value if negative
 *
 * */

int main (void) {
	puts("Programming is like building a multilingual puzzle");
	return(0);
}
