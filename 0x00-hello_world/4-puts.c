#!/bin/bash
#include <stdio.h>
/**
 * Entry-point: main
 *   *
 *    *Return zero if no errors, otherwise return non-zero value.
 *     */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
