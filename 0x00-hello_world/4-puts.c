#include <stdio.h>
/**
 * main- This serves as the entry point for the program
 *  
 * Return: this returns zero if no errors, otherwise it returns a non-zero value.
 *    */
int main(void)
{puts("\"Programming is like building a multilingual puzzle");
return (0);
}
